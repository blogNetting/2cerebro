---
title: Registro de decisiones
created: 2026-09-08
updated: 2026-09-25
tags: [meta, arquitectura]
zona: tecnico
---

Decisiones de arquitectura del wiki y correcciones del usuario, con fecha. Consultar antes de proponer cambios estructurales.

## 2026-09-08 — Creación del wiki

- Patrón LLM Wiki de Karpathy. Tres capas: fuentes brutas inmutables, wiki mantenido por el LLM, esquema en `AGENTS.md`.
- Metodología PARA: `proyectos/`, `areas/`, `recursos/`, `archivo/`. Más `fuentes/` para material bruto e `inbox.md` para captura.
- `AGENTS.md` como fuente única de verdad; `CLAUDE.md` sólo lo importa. Objetivo: compatibilidad con Codex y Gemini CLI.
- Clasificación automática, sin preguntar al usuario. Enlace bidireccional obligatorio. Índices por carpeta obligatorios desde la primera nota.
- Formato de nota: frontmatter YAML plano (title, created, updated, tags, zona), resumen de una línea, cuerpo. Ficheros cortos y monotemáticos.
- Operaciones: ingesta, consulta (`/buscar`), lint con síntesis proactiva.
- Subagentes: `archivista` (ingesta e inbox), `auditor` (lint y síntesis).
- Umbral de subdivisión de carpeta: 30 notas.

## 2026-09-09 — Estructura de proyectos con alcance abierto

- Cuando un proyecto arranca sin saber qué apartados tendrá, crear solo la nota hub del proyecto con una lista de "frentes de trabajo". Cada frente se saca a su propia nota corta cuando se aborda, no antes.
- Aplicado a [[apartamentos-calle-uruguay]].

## 2026-09-14 — Renombrado del proyecto del dúplex de Carballo

- El proyecto "Alquiler dúplex dividido en dos apartamentos" pasa a llamarse **Apartamentos Calle Uruguay**, nombre más simple ligado a la dirección (R/ Uruguay 2-4, Carballo). Fichero renombrado a `apartamentos-calle-uruguay.md`, actualizados los enlaces entrantes.
- Nuevo frente abierto en el hub: portero automático que no debe molestar a los dos inquilinos a la vez. Sin nota propia todavía — se crea cuando llegue el análisis, siguiendo la regla del apartado anterior.

## 2026-09-19 — Escalado de búsquedas web y navegador por CDP

- Investigación y decisión: ver [[entorno]] para el detalle de MCPs evaluados.
- Skills nuevas, organizadas por capacidad y no por sitio: `/investigar-web` (capas 1-2, `WebSearch`→`WebFetch`, autónoma) y `/navegador-cdp` (capa 3, Chrome real por CDP). Descartado un tercer skill `precio-producto`: el usuario decidió que la comparación de precio/producto es solo un criterio de búsqueda dentro de `investigar-web`, no una capacidad aparte — dos skills bastan.
- MCP elegido para el paso 3: **Playwright MCP** (Microsoft), conectado a un Chrome ya corriendo vía `--cdp-endpoint`. Descartado `chrome-devtools-mcp` (Google): también conecta por CDP a un navegador existente y cumple el criterio eliminatorio, pero está orientado a depurar (red, performance, consola) y no a conducir el navegador; cada tool de más consume contexto en cada turno sin aportar a este caso de uso. Descartado Puppeteer MCP (deprecado/archivado) y navegadores cloud tipo BrowserBase (lanzan su propio navegador, no el existente).
- Descartada la capa de datos de producto/precio sin scraping (Keepa API, SerpApi): requieren API key de pago y el usuario decidió no incluirla por ahora. Si se revisita, evaluar bajo el mismo criterio de "herramienta existente antes que script improvisado".
- Registrado en `.mcp.json` del proyecto: `playwright-cdp`, con `--cdp-endpoint=${CDP_ENDPOINT:-http://192.168.1.5:9222}`. Limitación asumida: el endpoint se fija al arrancar el proceso MCP; cambiar de máquina dentro de la misma sesión no es posible, hace falta relanzar `claude` con `CDP_ENDPOINT` puesto a la IP de la otra máquina.
- Regla añadida a `AGENTS.md`: reutilización de skills — comprobar si ya existe una antes de resolver algo o de crear una nueva, y registrar en cada skill qué alternativa se descartó y por qué.

## 2026-09-19 — Corrección: el escalado a CDP es obligatorio tras un bloqueo

- Fallo detectado por el usuario: en una consulta de precios, `WebFetch` devolvió HTML vacío en Amazon y 403 en Leroy Merlin y Bauhaus, y se entregó «no verificado» en vez de escalar a `/navegador-cdp`, pese a que `investigar-web` ya listaba esos casos. Además se leyó un escepticismo del usuario («sin navegador real no creo que los saques») como prohibición.
- Correcciones aplicadas: `investigar-web` paso 3 pasa a obligatorio; nuevos pasos 4-8 (un bloqueo no es un resultado; solo una prohibición explícita suspende el escalado; el sitio que el usuario prioriza se resuelve primero; los resúmenes del buscador no son precio; informar por partes). `AGENTS.md` (sección Reutilización de skills) alineado.
- `navegador-cdp`: nuevo paso 0. Si `CDP_ENDPOINT` ya está definida y responde a `curl`, esa es la máquina elegida y no se pregunta otra vez; la pregunta a/b solo aplica si la variable no está definida. Es un cambio sobre el diseño original («se elige por consulta»); revertible si el usuario prefiere preguntar siempre.
- Sitios comprobados y selectores de Amazon: ver [[entorno]].

## 2026-09-19 — Puente de contexto entre sesiones (VSCode ↔ CLI)

- Verificado en docs oficiales: la extensión de VSCode y el CLI mantienen almacenes de sesión separados; `claude --continue`/`--resume` no puede recuperar una conversación de la otra superficie. Sin solución nativa.
- Solución: directorio `.claude/sesiones/` (gitignored, fuera del PARA, no lo tocan `/lint` ni las reglas de clasificación de `AGENTS.md`). Skills `/exportar-sesion` y `/importar-sesion` — ver [[entorno]].
- Por defecto se vuelca un resumen de estado, no la transcripción literal: cuesta menos contexto a la sesión receptora. La transcripción completa queda disponible solo si se pide explícitamente.

## 2026-09-19 — Corrección: `.claude/` es solo configuración; los resultados de trabajo van a PARA

- Fallo detectado por el usuario: resultados de trabajo (búsqueda de alojamiento en Londres, vuelos Vueling) se dejaron dentro de `.claude/sesiones/`. `.claude/` es configuración (commands, agents, settings), nunca contenido.
- Correcciones: contenido migrado a `proyectos/` ([[viaje-londres-diciembre-2026]] y sus notas, [[vuelos-sevilla-octubre-2026]]) y a `areas/` ([[vueling-busqueda-por-url]]), con frontmatter, enlaces e índices. Los adjuntos (65 fotos y un PDF) van a `fuentes/londres-alojamiento-2026-09-19/`, ignorados por git: son fotos de terceros y el repo es público. `.claude/sesiones/` borrado. Regla nueva en `AGENTS.md` (sección Repositorio y artefactos).
- Aclaración del usuario: una exportación (`/exportar-sesion`) sirve solo para pasar contenido a una sesión nueva y se hace únicamente cuando él lo dice. No obliga a repetirla, ni a mantenerla actualizada, ni tiene que ver con el wiki. Las skills de exportar/importar no se tocan. Como siguen escribiendo en `.claude/sesiones/`, ese directorio queda ignorado por git como red de seguridad; si el usuario prefiere otro destino, habrá que cambiar las skills.

## 2026-09-19 — Corrección: artefactos de herramienta versionados y publicados

- Fallo detectado por el usuario: `.playwright-mcp/` (logs, capturas y snapshots del navegador) no estaba en `.gitignore` y `cerebro-sync.sh` (cron horario, `git add -A` y push) lo subió a GitHub: 140 ficheros, 11 MB, en los commits de las 19:00, 20:00, 21:00 y 22:00 UTC del 2026-09-19.
- Hallazgo: el repo `blogNetting/2cerebro` es público. Lo publicado con datos personales: nombre completo y nivel Genius de la cuenta de Booking; nombre y email de una cuenta de Google; la línea de envío de una cuenta de Amazon (nombre, localidad y código postal); 6 URLs de inicio de sesión de Booking con `op_token` en un log. Sin cookies, JWT, claves ni contraseñas. Las 5 capturas son páginas públicas de Vueling.
- Correcciones: `.playwright-mcp/` y `*.log` en `.gitignore`; carpeta desindexada y borrada; `.mcp.json` con `--output-dir=/home/netting/.cache/playwright-mcp` para que Playwright MCP escriba fuera del repo (aplica al reiniciar la sesión); regla en `AGENTS.md`: todo directorio de caché o artefacto va a `.gitignore` en cuanto aparece; comprobación nueva en `/lint` de ficheros versionados que no deberían estarlo.
- El historial de GitHub no se ha reescrito: pendiente de decisión del usuario.

## 2026-09-20 — Regla permanente: formato de investigaciones

- Petición del usuario tras una búsqueda de coche de alquiler Córdoba → A Coruña en la que faltaban enlaces («como siempre me faltan enlaces»). Toda investigación se entrega con una estructura fija: introducción, considerado y descartado (con motivo), análisis detallado y visual, recomendaciones, dónde se ha buscado (incluidas las fuentes que no aportaron) y otros aspectos relevantes.
- Fallo de fondo: los enlaces se omiten o se dejan solo al final. La regla exige un enlace por cada afirmación con fuente, en el punto donde aparece, además de URL directa por cada opción, y una revisión final antes de entregar. Dato sin fuente enlazable: «sin verificar» u omitido.
- Salida: `.md` por defecto, como nota del wiki según PARA; PDF solo si el usuario lo pide, generado desde el `.md`.
- Recogido en `AGENTS.md`, sección «Formato de investigaciones». Complementa `/investigar-web` (cómo buscar); no lo sustituye.

## 2026-09-24 — Sistema de desarrollo con agentes: decisiones de partida

- Proyecto nuevo: [[astillero]]. Genérico: debe servir para cualquier desarrollo; stack y plataforma se deciden en cada proyecto.
- El código de este sistema y de cada app vive en repos propios, nunca en `2cerebro` (es público y se publica cada hora). En el wiki, cada uno tiene una nota hub que enlaza al repo.
- Regla dura del usuario: no hay código sin tests, sean unitarios o de integración. Los umbrales de cobertura y complejidad se fijarán más adelante.
- Aceptado usar DeepSeek por su API oficial; que los datos estén en China no es un problema. El coste se controla con saldo prepagado y midiendo el consumo.
- La arquitectura Opus/DeepSeek se decide por investigación, pruebas y decisión conjunta, no por opinión.

## 2026-09-24 — Herramientas de investigación técnica

- Instalados `gh` y `yt-dlp` en `~/.local/bin`, binarios oficiales con el checksum verificado. No hay sudo; se descartó `apt`. Se descartaron también los MCP de GitHub y de YouTube: la CLI cubre lo mismo sin gastar contexto en definiciones de tools en cada turno.
- `/investigar-web` tiene un paso 10 nuevo con fuentes directas: GitHub, la API de Algolia de Hacker News y transcripciones de YouTube. Reddit y X van directos a CDP. El deep research de otras IAs solo sirve para descubrir pistas. Ver [[entorno]].

## 2026-09-25 — Sistema de desarrollo con agentes: correcciones y decisiones del usuario

- Subcarpeta `proyectos/sistema-desarrollo-con-agentes/` con su `_index.md`, aprobada por el usuario («tiene que estar en una carpeta propia»). Contiene las 11 notas del proyecto. Los wikilinks no cambian porque los nombres se mantienen.
- El ejecutor es abstracto: la arquitectura no depende de quién implemente. DeepSeek se usa por coste, no por calidad. Corrección: en [[desarrollo-agentes-investigacion]] se trató DeepSeek como si hubiera que justificarlo por calidad.
- Decisiones: GitHub como forja; framework spec-driven elegido en cada proyecto; no hay piloto hasta tener el sistema completo.
- Correcciones de forma: explicar cada tecnicismo al usarlo (DORA se usó sin explicar); ante una pregunta amplia, traer una propuesta concreta en vez de devolver la pregunta; no desviarse del problema central (diseño en tareas → cola de tareas tipo Beads → ejecutores → revisión).

## 2026-09-25 — Regla permanente: lo que menciona el usuario no condiciona nada

- Petición expresa del usuario («escríbelo en sangre»), tras varias repeticiones del mismo fallo: tomar lo que él nombra (DeepSeek, issues, gitflow, Beads…) como premisa o como centro del trabajo.
- Se añade la sección «Lo que menciona el usuario no condiciona nada» a `AGENTS.md`, junto con la regla de explicar cada sigla al usarla (ajuste de «no expliques fundamentos»).

## 2026-09-25 — Flujo de desarrollo con agentes: diseño propuesto (pendiente de revisión del usuario)

- Investigación en fases A–C con revisión del orquestador entre fases. Se corrigieron dos citas no literales, un inventario incompleto y un benchmark con fecha imposible ([[flujo-agentes-informe]] §2).
- Propuesta: GitHub Issues + sub-issues + dependencias como estado; primitivas de GitHub Actions (grupos de concurrencia, `timeout-minutes`) para reserva y caducidad; gh-aw como ejecutor con DeepSeek a través del endpoint compatible con Anthropic; `claude-code-action` con token de la suscripción Pro para que revise Opus; CI determinista y `CODEOWNERS`. Se descarta Beads por R13 (fallos abiertos de corrupción y demonios).
- Todavía no es una decisión cerrada: la valida el usuario. Detalle en [[flujo-agentes-arquitectura]].

## 2026-09-25 — Nombre del repo plataforma: Astillero. Mecanismo de replicación verificado

- El repo que contendrá los workflows y la configuración reutilizables del sistema se llamará **Astillero** (decisión del usuario, tras descartar varias tandas de nombres propuestos).
- Mecanismo de replicación a cada proyecto, verificado en fuente primaria: reusable workflows de GitHub Actions (`workflow_call`, confirmado que funcionan entre repos privados de una cuenta personal) para el revisor y el reconciliador; imports remotos de gh-aw (`imports: owner/repo/path@ref`, confirmado que existen) para el ejecutor; **copier** para lo estático (`CODEOWNERS`, `AGENTS.md`, labels, plantilla de contrato), descartados cruft y `repo-file-sync-action` por falta de mantenimiento. Detalle completo en [[astillero-replicacion]].
- **Corrección del usuario (2026-09-25):** todo repo de este ecosistema (Astillero y cada proyecto, empezando por Patrimonial) se crea **privado**, siempre, sin plantear la opción pública. Consecuencia: hace falta GitHub Pro (~4 $/mes, cubre todos los repos privados de la cuenta, no es coste por repo) para tener rulesets y merge queue en cualquiera de ellos.
- Sin decidir todavía: la creación real del repo Astillero en GitHub (hoy solo existe el diseño).

## 2026-09-25 — Renombrado: sistema-desarrollo-con-agentes → Astillero

- Otra sesión de esta cuenta decidió el nombre **Astillero** para el repo plataforma y ya lo había creado en GitHub (`blogNetting/astillero`, privado) e investigado su mecanismo de replicación ([[astillero-replicacion]]: reusable workflows para revisor/reconciliador, imports remotos de gh-aw para el ejecutor, copier para lo estático), antes de que esta sesión lo supiera.
- Esta sesión renombró la carpeta y la nota hub del wiki (`sistema-desarrollo-con-agentes` → `astillero`) para que coincidan, sin perder ningún enlace ni contenido. Verificado que no hubo colisión de ediciones concurrentes (el único solape, en `flujo-agentes-arquitectura.md`, era una edición propia de esta sesión capturada por el cron de las 20:00, no un cambio ajeno).
- A partir de aquí, «Astillero» es el nombre único del proyecto en el wiki y en GitHub.

## 2026-09-25 — Capa de producto: diseño cerrado, Astillero completo

- Petición del usuario: «quiero crear software como si fuera una empresa, hacer de product owner... alguna pregunta porque luego no voy a aceptar cagadas». Aclarado por el propio usuario: enfoque corporativo y serio, no departamentos no técnicos ni ceremonia de equipo; «mientras funcione me da igual».
- Investigación dirigida activamente a encontrar el patrón contrario y no encontrarlo: **cero operadores solos usando RICE/ICE/WSJF** (hilo de HN de 130 puntos, ~30 operadores solos, sin ninguna mención); **la mesa de apuestas de Shape Up exige pluralidad de personas por diseño del propio libro** («one designer and one programmer» como mínimo) — ninguna de las dos prácticas se adopta.
- Decisiones: sin scoring de priorización, backlog simple reordenado por juicio directo; captura de idea vía entrevista (patrón oficial de Anthropic) con Appetite y No-gos de Shape Up como únicos campos que sí sobreviven; panel en GitHub Projects v2 vía `update-project`/`create-project-status-update` extendiendo el reconciliador — pieza que `github/gh-aw` documenta pero no usa para sí mismo (verificado dos veces: 0 proyectos en la organización, badges en su lugar); bugs con triage de solo-reproducción (patrón real de Metabase, Repro-Bot, ~10% falsos positivos citado por su autor) antes de entrar al mismo contrato de tarea que una funcionalidad, sin atajos de hotfix; versionado por checkpoint (`release-please` o notas nativas de GitHub) en vez de publicación automática; registro de decisiones de producto sin precedente real encontrado (inferencia explícita, ni siquiera Shape Up documenta sus propios descartes). Detalle completo en [[capa-producto]].
- Con esto, Astillero queda documentado como sistema completo: motor de ingeniería ([[flujo-agentes-arquitectura]], probado en vivo), replicación ([[astillero-replicacion]], probada en vivo) y capa de producto ([[capa-producto]], diseñada con evidencia, sin implementar en código todavía). Nota hub `astillero.md` actualizada para reflejar las tres piezas como una sola imagen coherente.

## 2026-09-25 — Auditoría real: el ciclo no llegaba a producción. Despliegue y DevOps mínimo, cerrados

- Pregunta directa del usuario: «¿todo lo que tienes cubre todo el proceso de idea a implementación?». Respuesta obtenida por auditoría real (lectura completa de las 15 notas del proyecto, no reafirmación): **no** — el pipeline construido hasta ese momento terminaba en el merge a `main` (K11); cero notas cubrían despliegue a producción, observabilidad en producción o gestión de incidentes. Confirmado también que el sistema de cobertura de tests, al contrario de lo que sugería una línea desactualizada de `astillero.md`, **ya estaba resuelto** desde el 2026-09-24 en `desarrollo-agentes-f4-devsecops.md` §3.3 — solo mal enlazado desde el hub.
- Cerrados hoy, con la misma exigencia de evidencia que el resto del proyecto:
  - **Despliegue a producción** ([[flujo-agentes-arquitectura]] §15): CD automático en cada merge a `main`, sin checkpoint manual aparte de la versión (`release-please` sigue siendo un eje distinto, no bloquea el CD) — evidencia de 40+ operadores solos en dos hilos de HN. Dónde corre la app: PaaS gestionado, PaaS autoalojado sobre VPS (Dokku/CapRover), o VPS+Compose puro, sin staging permanente por defecto. Rollback: revertir el commit, extiende K10. Migraciones: expandir y contraer el schema, siempre dos tareas distintas del backlog (regla dura, ligada al hecho de que el ejecutor trabaja tarea por tarea).
  - **DevOps mínimo en producción** ([[devops-minimo]], nota nueva): anclado en un caso real auditable, no en opinión — Healthchecks.io, SaaS operado en solitario con su stack de producción publicado y verificado (repo, historial de commits). Hallazgo contraintuitivo con fuente: **no hace falta on-call formal con un solo operador** — diseñar para resiliencia (auto-restart, alerta que despierte) basta; el propio hilo de referencia de 149 puntos en HN lo confirma en consenso mayoritario. Backup diario cifrado + replicación con failover manual deliberado (no automático — "problema demasiado difícil" según la propia fuente). Secretos con `sops`, umbral real: por debajo de ~10 repos basta, por encima hace falta Vault — Astillero está muy por debajo. Parcheo de dependencias: el caso real auditado ni siquiera usa Dependabot, se apoya en CI real en cada commit — válido también aquí.
  - **Correcciones al sistema de cobertura ya cerrado**: Vitest usa su proveedor `v8` nativo, no `c8` como paquete aparte (desactualizado); Codecov en vez de Coveralls, porque Coveralls no tiene plan gratis para repos privados y todo repo de Astillero es privado por decisión ya cerrada; sin umbral global fijo de cobertura por ser gameable (Fowler, fuente primaria del propio término), `patch` (líneas nuevas) cerca del 100% como gate real, apoyado en mutation testing del diff ya decidido.
- Bloqueo real encontrado y no escondido: la cuota de `WebSearch` de la sesión se agotó (200/200) durante estos frentes de investigación — resuelto con la API de HN Algolia y `gh api`/`gh search` sobre repos reales en vez de descubrimiento por buscador, sin que afectara a ninguna conclusión (todo lo citado viene de fuente primaria verificada, no de la búsqueda en sí).
- Astillero queda con 5 piezas documentadas: motor de ingeniería, replicación, capa de producto, despliegue y DevOps mínimo — las dos últimas diseñadas con evidencia real pero **sin ejecutar en vivo todavía**, señalado explícito en `astillero.md`, no escondido bajo una reafirmación de "todo completo".

## 2026-09-25 — Segunda auditoría (a petición expresa): higiene, coherencia lógica y mantenimiento

- Petición del usuario: «reitero analizalo todo de nuevo en busca de fallos, no quiero que mañana me digas cagada en esto» — auditoría dedicada, con tres frentes paralelos, sobre todo lo construido hasta ahora.
- **Higiene del wiki** (enlaces, huérfanas, índices, frontmatter): limpio en general, pero 4 contradicciones internas reales encontradas y corregidas: (1) `astillero.md` afirmaba en una sección que el repo `blogNetting/astillero` "solo tiene el README" mientras otra sección, más nueva, decía que ya tenía los workflows publicados — verificado en vivo con `gh api` que la versión correcta es la segunda, corregido; el mismo error estaba también en `astillero-replicacion.md` §6. (2) Contradicción sobre cobertura: una línea decía "se fijará más adelante" cuando ya estaba cerrada desde ayer — corregida. (3) Cita errónea "extiende K10" para rollback, dos sitios: K10 es el contrato de merge (§4), no la sección de recuperación (§10) — corregido en ambos ficheros. (4) Sección "Bloques" del primer día, redundante y en tensión con el "Estado" reescrito — marcada como histórica.
- **Coherencia lógica del diseño** (no formato, mecanismo): 3 fallos reales. (1) El panel de PO (`capa-producto.md` §3) estaba diseñado con `safe-outputs.update-project`, pero el reconciliador es un `.yml` normal, no un workflow gh-aw compilado — `safe-outputs` no existe fuera de gh-aw. Corregido: el reconciliador llama directo a `gh project item-edit`/`gh api graphql` con el PAT, sin pasar por `safe-outputs`. (2) El runbook no listaba los secretos nuevos que el propio diseño de hoy necesita (`GH_AW_WRITE_PROJECT_TOKEN`, `CODECOV_TOKEN`) — añadidos a `flujo-agentes-runbook.md`. (3) Hueco real en la máquina de estados: "issue cerrada" (K11) no es lo mismo que "ya desplegado en producción" — con CD asíncrono, la tarea de "contraer" schema podía desbloquearse antes de que el despliegue de "expandir" estuviera confirmado en verde. Cerrado con un chequeo adicional del reconciliador en `flujo-agentes-arquitectura.md` §15.
- **Mantenimiento y propagación a proyectos existentes**, nota nueva [[astillero-mantenimiento]]: los imports de gh-aw y las plantillas de copier se resuelven en tiempo de COMPILACIÓN, no de ejecución — un proyecto ya bootstrapped no recibe cambios de `blogNetting/astillero` solo por apuntar a `@main`, hace falta un workflow propio (`astillero-update.yml`, cron semanal) que recompile y corra `copier update`. Hallazgo real que cambia el runbook: el `GITHUB_TOKEN` por defecto no puede tocar `.github/workflows/` — hace falta un PAT con scope `workflow` para ese mecanismo de sincronización. Protocolo de convivencia wiki↔código escrito por primera vez (era práctica de facto, nunca regla explícita): decisión primero en el wiki, implementación siempre en el repo real, la nota se marca "probada en vivo" solo con evidencia concreta.
- Nada de esto se escondió ni se suavizó: cada fallo se reporta con la nota y sección exacta donde estaba, y la corrección aplicada en la misma operación.

## 2026-09-26 — Punto de entrada único: skill `/astillero-proyecto`

- Petición del usuario: un "formulario de iniciación" que detecte si un proyecto ya existe (aunque sea a medias) antes de preguntar nada, y solo pida o genere lo que falta. Corrección propia en el diseño previo: había mezclado en una sola lista de "básico" cosas de naturaleza distinta — se separó en imprescindible (identidad, repo, nota hub, etiquetas de estado — sin esto la máquina de estados no tiene dónde apoyarse), de calidad con default razonable (ecosistema, bootstrap de copier) y "fase de uso, no de arranque" (captura de idea, secretos — no bloquean la creación del proyecto).
- Verificado con el agente especializado en Claude Code, no asumido: no existe `~/.claude/commands/` global — los comandos de `2cerebro/.claude/commands/` solo funcionan abriendo Claude Code dentro de ese repo. El mecanismo real para algo invocable igual desde cualquier sesión es un **skill** en `~/.claude/skills/<nombre>/SKILL.md`.
- Creado `~/.claude/skills/astillero-proyecto/SKILL.md` (fuera del repo del wiki, deliberado: tiene que funcionar también desde la sesión de Patrimonial y de cualquier proyecto futuro). Confirmado en vivo: el skill aparece en la lista de skills disponibles de la sesión sin reiniciar nada.
- Sin probar en vivo todavía — la primera invocación real queda para una sesión de Patrimonial, explícitamente fuera de esta.

## 2026-09-26 — Cierre real de los 4 huecos del bootstrap: rehacer, CI/CD, triage de bugs, CODEOWNERS roto

- El usuario exigió cerrar sin errores, con estudio de fallos, lo que la auditoría anterior había dejado como "falta" en el `template/` de copier: `rehacer.md` no se propagaba a proyectos nuevos (existía en `blogNetting/astillero` pero no en `template/`), no había ni `ci.yml` ni workflow de despliegue en ningún sitio (solo diseñados en la wiki), el triage de bugs (`capa-producto.md` §4) tampoco existía como fichero. Al implementar `CODEOWNERS.jinja` se encontró además un bug real no detectado antes: un ternario de Jinja que devolvía siempre el mismo valor en las dos ramas, y `copier.yml` nunca preguntaba usuario de GitHub — el fichero generado salía con `@TU_USUARIO` literal sin rellenar en todas las líneas.
- Cerrado con evidencia real, no solo con ficheros escritos: 6 SHA de acciones de terceros verificados contra la API de GitHub el mismo día (`actions/checkout`, `actions/setup-python`, `actions/setup-node`, `codecov/codecov-action`, `gitleaks/gitleaks-action`, `google/osv-scanner-action`); `copier.yml` con la pregunta `github_username` nueva; `CODEOWNERS.jinja` corregido; `template/.github/workflows/{rehacer.md,ci.yml,deploy.yml,reproducir-bug.yml}.jinja` nuevos; `.github/workflows/reproducir.yml` (reusable, Opus, solo lectura) nuevo en `blogNetting/astillero`; `contrato-tarea.md.jinja` movido a `template/docs/` (sin convención externa que lo fijara en la raíz).
- **Simplificación real encontrada al implementar, no solo un hueco:** la puerta "tests intactos" de §8 no necesita script de CI propio — ya la cubre `CODEOWNERS` de forma nativa vía el ruleset, sin pieza nueva. Se retiró de la tabla de puertas como fila propia.
- Verificado en vivo, de extremo a extremo, dos veces (python y node): `copier copy gh:blogNetting/astillero` genera los 10 ficheros esperados, `CODEOWNERS` sale sin `TU_USUARIO`, los 5 YAML planos parsean correctos, y `gh aw compile` compila `implementar.md`/`rehacer.md` en los dos ecosistemas — confirmado además que `network.allowed` inyecta `pypi.org`/`files.pythonhosted.org` en python y `npmjs.org`/`registry.npmjs.org` en node, no un valor fijo.
- `deploy.yml` queda con el gate de `environment: production` funcional pero el paso de despliegue real como placeholder explícito que falla a propósito — el destino (VPS/Fly.io/Dokku) sigue sin ser decisión de Astillero, es por proyecto (§15). No se fingió que esto estuviera resuelto.
- Repos de prueba (`/tmp`) borrados tras verificar; el commit de prueba en `blogNetting/prueba-flujo-agentes` (subcarpeta de workflows, para comprobar empíricamente que GitHub Actions no las detecta) revertido en el mismo repo.

## 2026-09-26 — Decisión cerrada: Astillero no se desarrolla a sí mismo con DeepSeek (sin dogfooding)

- Error propio detectado por la sesión de Patrimonial, no por mí: horas antes, al probar en vivo que la Fase 1 del skill `/astillero-proyecto` detectaba huecos reales, apliqué el criterio de "etiquetas de estado imprescindibles" también a `blogNetting/astillero` y las creé ahí — sin pararme a decidir si Astillero debía desarrollarse con su propio pipeline barato. Aplicar un criterio pensado para proyectos que *consumen* la plantilla a la plantilla misma, sin examinarlo.
- Decisión, con motivo, no por instinto: **no.** Astillero es la pieza de mayor radio de explosión de todo el sistema — sus workflows se importan por referencia (`imports: @main`) en todos los proyectos, resueltos en tiempo de compilación; un cambio malo mergeado ahí se propaga a lo que recompile después. DeepSeek se eligió por coste, no por calidad — la elección correcta para una tarea acotada en un proyecto cualquiera, la equivocada para la pieza que controla a todas las demás. Precedente real ya verificado en esta misma investigación: el propio `github/gh-aw`, construido por el equipo que lo mantiene, dogfoodea sobre sí mismo y tiene fallos documentados en producción (`duplicate-code-detector` fallando en silencio desde el 18 de septiembre) — ni su creador lo tiene resuelto de forma fiable sobre infraestructura crítica propia.
- Corregido: etiquetas `estado:*`/`agente:*` eliminadas de `blogNetting/astillero` (vuelto a las etiquetas por defecto de GitHub). No se completan `AGENTS.md`, secretos ni `docs/contrato-tarea.md` en su raíz — su ausencia es correcta, no un hueco.
- `~/.claude/skills/astillero-proyecto/SKILL.md` actualizado: instrucción explícita de no aplicar nunca el inventario/bootstrap de las Fases 1-2 a `blogNetting/astillero`, y de avisar si el modelo activo no es Opus antes de cualquier paso de diseño (Fase 4 / decisiones de arquitectura) — el ajuste global de Claude Code es Sonnet por defecto y no cambia solo.
- Reversible: si en el futuro se quiere reabrir esto (por ejemplo, solo para mantenimiento menor, con `CODEOWNERS` propio de Astillero bien puesto primero), se evalúa entonces con el mismo rigor — no por defecto.

## 2026-09-26 — Bug real encontrado por la sesión de Patrimonial: `.copier-answers.yml` nunca se generaba

- La sesión de Patrimonial detectó, de forma reproducible, que `copier copy` sobre la plantilla de Astillero no dejaba `.copier-answers.yml` en el proyecto generado — lo mitigó a mano solo en Patrimonial y avisó aquí para corregir el origen, sin tocar la plantilla ella misma (regla de reparto ya acordada: Patrimonial no toca Astillero).
- Verificado por mí, reproducido en un directorio limpio antes de tocar nada: confirmado, no se generaba. Causa real, no supuesta — confirmada contra la documentación oficial de copier (sección "creating"): `.copier-answers.yml` **no lo escribe copier automáticamente**, tiene que ser la propia plantilla la que incluya un fichero con el nombre exacto `{{_copier_conf.answers_file}}.jinja` (la expresión Jinja va en el nombre del fichero) y contenido `# Changes here will be overwritten by Copier` + `{{ _copier_answers|to_nice_yaml -}}`. Ese fichero nunca se añadió al construir la plantilla.
- **Por qué esto era grave, no cosmético:** todo el mecanismo de actualización con tu aprobación diseñado hoy (`copier update`, versiones etiquetadas, `astillero-update.yml`) depende de que `.copier-answers.yml` exista y registre `_commit`/`_src_path` — sin él, ningún proyecto generado desde Astillero podría recibir una actualización nunca, tendría que reconstruirse a mano cada vez, como tuvo que hacer la sesión de Patrimonial.
- Corregido en `blogNetting/astillero` (commit `dd206d7`), verificado en vivo generando un proyecto limpio: `.copier-answers.yml` sale con `_commit: dd206d7`, `_src_path` y las tres respuestas. Patrimonial sigue con el fichero reconstruido a mano por la otra sesión — no hace falta tocarlo, seguirá funcionando igual la próxima vez que reciba una actualización real.

## 2026-09-26 — Documentación del repo (`README.md`, `AGENTS.md.jinja`) al día; versionado real implementado

- Fallo propio detectado por el usuario, no por auditoría: `README.md` de `blogNetting/astillero` llevaba desde el 2026-09-25 sin tocar — 3 enlaces rotos a la carpeta vieja del wiki, estado contradictorio consigo mismo, sintaxis de Obsidian que no renderiza en GitHub. `AGENTS.md.jinja` tenía dos secciones vacías (solo comentarios HTML) pese a que ya había investigación real sobre cómo estructurar este fichero (`flujo-fase-a2-practica-a-escala.md`, patrón de OpenAI: ~100 líneas, índice no manual). Corregidos los dos, con el contenido ya investigado, no inventado de nuevo — commit `b65706f`.
- **Versionado real implementado, no solo diseñado.** Hasta hoy todo apuntaba a `@main`, incluidos los reusable workflows (`revisar`/`reconciliar`/`reproducir-bug`), que SÍ se actualizaban solos en cada ejecución — justo lo que el usuario no quería. Cerrado: nueva pregunta de copier `astillero_ref` (tag real), las 5 plantillas de `.github/workflows/*.jinja` referencian `@{{ astillero_ref }}` en vez de `@main`. Primer tag real cortado: `v0.1.0` (commit `7785ed1`).
- **Bug propio encontrado y corregido en el mismo proceso, antes de subir nada:** un `sed` inicial rompió la línea `{{#runtime-import ...}}` de `implementar.md.jinja`/`rehacer.md.jinja` al meter `{{ astillero_ref }}` dentro de una cadena literal de Jinja (no se evalúa recursivamente) — corregido con concatenación (`~`) fuera de las comillas. Detectado verificando el fichero `.md` generado de verdad, no solo que `copier`/`gh aw compile` no dieran error.
- **Segundo bug propio, de proceso**: se cortó el tag `v0.1.0` antes de terminar el cambio de `astillero_ref`, así que la primera verificación (contra el directorio local) dio falso positivo silencioso — `copier` resolvió al tag viejo sin el cambio, no al working tree. Detectado al no encontrar `@v0.1.0` en el fichero generado; corregido moviendo el tag al commit correcto y re-verificando contra `gh:blogNetting/astillero@v0.1.0` (GitHub real, no ruta local) antes de dar el cierre por bueno.
- `release-please` instalado en `blogNetting/astillero` (`.github/workflows/release-please.yml`, `release-please-config.json`, manifest arrancando en `0.1.0`) — versiones futuras se cortan aprobando una PR, no a mano.
- Regla que se aplica a partir de ahora, no solo se declara: antes de dar por cerrado cualquier cambio en Astillero, revisar TODOS los ficheros que describen o forman parte de lo tocado (no solo los editados), y verificar el resultado generado de verdad (el fichero final), no solo que el comando no diera error.

## 2026-09-25 — Patrimonial: carpeta y repo propios, adelantados a la regla de espera

- Corrección del usuario: la nota vivía como `app-seguimiento-patrimonio.md` suelta en `proyectos/`, con nombre distinto al decidido («Patrimonial») y sin repo. Movida a `proyectos/patrimonial/patrimonial.md`, con `_index.md` propio; todos los enlaces entrantes actualizados (`apartamentos-calle-uruguay`, `fiscalidad-alquiler-por-habitaciones`, `proyectos/_index`, y las notas de Astillero que ya la citaban).
- Repo creado: [blogNetting/patrimonial](https://github.com/blogNetting/patrimonial), privado, clonado en `~/dev/patrimonial`. Esto adelanta la regla de 2026-09-09 («solo la nota hub hasta que se aborde cada frente») porque el usuario lo pidió explícitamente, y porque Astillero ya se documenta como completo — el bloqueo «no hay piloto hasta que el sistema esté listo» queda superado en la práctica, pendiente solo de que el usuario lo confirme de palabra.
- Sin código todavía: Patrimonial sigue en toma de requisitos (modelo de entidades cerrado, stack y despliegue sin decidir).

## 2026-09-26 — Regla permanente: qué va al wiki y qué al repo de cada proyecto de Astillero

- Corrección del usuario sobre [[patrimonial]]: conocimiento técnico reusable entre proyectos (y todo lo que conecta con vida no técnica) va al wiki; lo específico y adaptado de un proyecto concreto (entidades exactas, stack, contratos de tarea) va a su propio repo, no se duplica en los dos sitios.
- Aplicación: mientras un proyecto está en entrevista de requisitos, sin framework spec-driven elegido, el detalle vive en su nota hub del wiki porque no existe todavía el `specs/` del repo al que trasladarlo. En cuanto arranca el diseño formal (contrato K1 de [[flujo-agentes-arquitectura]]), ese detalle se traslada a `specs/<proyecto>/spec.md` en el repo, y la nota del wiki se resume a hub.
- Recogido como principio permanente en [[flujo-agentes-arquitectura]] §1, y anotado como pendiente explícito en [[patrimonial]] para que no dependa de que alguien se acuerde.

## 2026-09-26 — Astillero no se desarrolla a sí mismo con DeepSeek

- Decisión del usuario, al inicializar Astillero con la skill `astillero-proyecto`: los cambios a Astillero (sus workflows, su plantilla) se hacen **a mano, por el usuario y por Opus**, nunca con el pipeline barato (DeepSeek). Sin dogfooding.
- Consecuencia: no se rellenan `AGENTS.md`, secretos (`DEEPSEEK_API_KEY`, `CLAUDE_CODE_OAUTH_TOKEN`) ni `docs/contrato-tarea.md` en la raíz de `blogNetting/astillero` — su ausencia es intencional, no un hueco por completar.
- El usuario quitó a mano las etiquetas `estado:*`/`agente:*` que la otra sesión había puesto de más en ese repo (sin querer, sugerían un pipeline activo que no debe existir ahí).

## 2026-09-26 — Patrimonial: bootstrap con la plantilla de Astillero, y un bug real encontrado en ella

- Arrancado con la skill `astillero-proyecto`: etiquetas de estado creadas en `blogNetting/patrimonial`, bootstrap de `copier` aplicado (`AGENTS.md`, `CODEOWNERS` correctamente rellenado con `@blogNetting`, `docs/contrato-tarea.md`, workflows de ejecutor/revisor/reconciliador/CI/despliegue), ecosistema Python. Compilado con `gh aw compile --approve` tras verificar en el propio `.lock.yml` que el secreto nuevo (`DEEPSEEK_API_KEY`) coincide exactamente con el diseño documentado, sin nada añadido de más.
- **Bug real encontrado en la plantilla de Astillero, no corregido aquí** (esta sesión tiene alcance limitado a Patrimonial; el usuario lo está trabajando en otra ventana): `copier copy gh:blogNetting/astillero` no genera `.copier-answers.yml` en el destino — comprobado 3 veces en directorios limpios, con y sin `--answers-file` explícito, siempre exit 0 y siempre sin el fichero. Sin él, el mecanismo de actualización continua de [[astillero-mantenimiento]] (`copier update --defaults`) no tiene de dónde partir en ningún proyecto que use esta plantilla.
- Mitigación aplicada solo en Patrimonial, para no bloquearlo: `.copier-answers.yml` reconstruido a mano con los mismos valores exactos de la copia real (`project_slug: patrimonial`, `package_ecosystem: python`, `github_username: blogNetting`, `_commit`/`_src_path` del template).
- **Corrección de la propia sesión: el usuario pegó la clave real de DeepSeek en el chat.** Tratada como comprometida, no usada — se le indicó regenerarla en DeepSeek y meterla ella misma con `gh secret set` (prompt interactivo, nunca en el chat), igual para el token de Claude Code (`claude setup-token`, ejecutado por el usuario, no por la sesión).
- **Bug de `.copier-answers.yml` resuelto solo, sin intervención**: la otra ventana publicó el primer tag real de Astillero (`v0.1.0`) y una pregunta nueva de copier (`astillero_ref`, para fijar versión en vez de `@main`). Al re-ejecutar `copier copy ... --vcs-ref v0.1.0` sobre un repo ya con tag, el fichero de respuestas se generó correctamente — confirma que la causa era la ausencia de tags, no un bug de la plantilla en sí.
- Patrimonial re-generado y recompilado fijado a `v0.1.0` (antes apuntaba a `@main` en 6 sitios, detectado y corregido en la misma operación); secretos `DEEPSEEK_API_KEY` y `CLAUDE_CODE_OAUTH_TOKEN` confirmados por nombre. Arranque de Patrimonial completo — sin código de aplicación, pendiente solo de la entrevista de diseño formal.
- **Precisión sobre la causa de `.copier-answers.yml`:** la entrada de arriba concluye que la causa era "ausencia de tags", coincidencia temporal con que el tag `v0.1.0` se cortara ese mismo momento. La causa real, confirmada contra la documentación oficial de copier en la sesión de Astillero: faltaba el fichero de plantilla `{{_copier_conf.answers_file}}.jinja` — sin tags también habría fallado igual. Ver entrada "Bug real encontrado por la sesión de Patrimonial" más arriba.

## 2026-09-26 — Contenido real de la plantilla: SECURITY.md, plantilla de bug, y versionado con `release-please` en vivo

- Corrección directa del usuario: dentro de Astillero había investigación real ya hecha en el wiki (checklist DevSecOps de `desarrollo-agentes-f4-devsecops.md` §3.4; diseño de triage de bugs de `capa-producto.md` §4) que nunca se materializó en la plantilla que de verdad se replica a cada proyecto — se quedó citada, no copiada. «Es tu puto trabajo» — dicho, con razón.
- Añadido a `template/`, con contenido ya investigado, no inventado: `docs/SECURITY.md.jinja` (checklist real, sourced de OWASP DevSecOps Guideline + OSPS Baseline, ya en el wiki); `.github/ISSUE_TEMPLATE/bug.yml.jinja` (issue form nativo con `labels: [tipo:bug]` automático, dispara `reproducir-bug.yml`, diseñado ayer y nunca construido); `AGENTS.md.jinja` con la sección "Estructura del repo" ya no vacía, apunta a lo real (incluido `SECURITY.md`) y dice explícito qué NO se genera de entrada (patrón OpenAI: `design-docs/`, `exec-plans/`, `product-specs/` — ceremonia sin uso hasta que haga falta).
- **Versionado real implementado de extremo a extremo, no solo el tag**: `release-please` (ya instalado) abrió sola la PR real **#1 "chore(main): release 0.2.0"** en `blogNetting/astillero` tras el primer push con commits `feat:` — con changelog generado, sin tocarla yo, a la espera de aprobación del usuario. Confirma que todo el mecanismo diseñado hoy funciona en producción, no solo en teoría.
- Dos bugs propios, encontrados y corregidos en el mismo proceso, antes de dar nada por cerrado: (1) release-please falló la primera vez por el mismo permiso de Actions para crear PRs que ya se había resuelto antes para otro repo, nunca activado aquí — corregido con `gh api .../actions/permissions/workflow`. (2) Verificación con falso positivo: probar contra un directorio git local con un tag antiguo hace que `copier` resuelva al tag, no al working tree — hay que forzar `--vcs-ref HEAD` para probar cambios sin commitear a un tag nuevo, o verificar siempre contra el remoto real.
- **Fallo real y directo del usuario, con motivo:** «en la version v0.1.0 lo que hay es una puta mierda». `v0.1.0` se cortó ANTES del commit con `SECURITY.md`/la plantilla de bug — no las tenía, y yo seguía diciendo a todo el mundo (skill, runbook) que usara esa versión. Corregido: mergeada la PR de `release-please` (`v0.2.0`), verificado con un `copier copy` real contra ese tag que sí contiene ambas piezas, y actualizadas las tres referencias (`SKILL.md`, `flujo-agentes-runbook.md`, `astillero-mantenimiento.md`).
- **Segundo fallo, más grave, mismo patrón — sobre `AGENTS.md.jinja`:** el usuario pegó el contenido exacto de vuelta y preguntó si le estaba vacilando. Cierto: había re-escrito "Cómo compilar y testear" y "Convenciones" con más texto explicando "rellena esto después", pero seguían siendo comentarios HTML vacíos — el mismo hueco de antes, mejor envuelto, presentado como "corregido" sin estarlo. Corregido de verdad: "Cómo compilar y testear" ahora trae comandos reales por ecosistema (python/node), reusando exactamente lo que ya corre `ci.yml`, no inventado; regla nueva en "Reglas duras" — Conventional Commits obligatorio, hueco real encontrado al revisar esto (`release-please`, instalado hoy, necesita ese formato de prefijo para funcionar y no se lo decía a nadie). "Convenciones" mantiene un hueco, pero con la razón dicha explícita (estilo de nombres dentro de `src/` es genuinamente desconocible de antemano), no un comentario vacío sin más.
- Mergeada también la PR resultante (`v0.2.1`), verificado con `copier copy` real contra ese tag, y actualizada la versión en las tres referencias otra vez, para no dejar la misma trampa dos veces.
- Lección que se queda, no solo se dice: "más texto explicando por qué está vacío" no es lo mismo que "contenido real" — antes de decir que una sección "ya no está vacía", comprobar que tiene una instrucción, dato o comando ejecutable de verdad, no una explicación de por qué no lo tiene.
- **Segunda vez que el usuario pide esto — pasa a regla permanente en `AGENTS.md`**, no queda solo aquí: «No decir "ya está" sin haberlo comprobado de verdad» (abrir el resultado final antes de decir que algo está arreglado; un comentario largo explicando por qué está vacío sigue siendo un hueco vacío; comprobar contra el repo real, no una copia local a medias).
- **Corrección inmediata del usuario sobre la redacción de esa misma regla**: la primera versión estaba escrita con el mismo estilo enrevesado que ya había generado quejas antes («prueba de fuego», «artefacto final») — no se entendía. Reescrita en lenguaje llano con ejemplos reales anclados a lo que acababa de pasar, sin idiomas ni jerga.
- **Segunda regla permanente añadida en el mismo momento**: «Toda pregunta se contesta, ninguna se salta» — si el usuario hace una pregunta directa, o varias en el mismo mensaje, se contestan todas explícitamente antes de cualquier otra cosa; si algo no se puede responder, se dice, no se calla.
